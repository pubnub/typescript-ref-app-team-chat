import React from "react";
import Login from "features/authentication/Login";
import { RouteComponentProps } from "@reach/router";

const LoginRoute = (props: RouteComponentProps) => {
  return <Login />;
};

export default LoginRoute;
